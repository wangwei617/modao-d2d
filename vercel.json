{
  "buildCommand": "sh ./build.sh",
  "installCommand": "echo 'Skipping root install, handled in build.sh'",
  "outputDirectory": "vercel-dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/app/:match*",
      "destination": "/app/index.html"
    }
  ]
}
